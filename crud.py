# from database import Session,engine
# from models import Basic_Details

# db = Session()

# def get_basic_details_of_applicant_by_id(applicant_id):
#     return db.query(Basic_Details).filter(Basic_Details.basic_details_id==applicant_id).first()

